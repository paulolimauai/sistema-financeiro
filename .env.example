# Copie este arquivo para ".env" e preencha com os dados do SEU PostgreSQL local
# (os mesmos dados que você usa para abrir a conexão "PostgreSQL 18" no pgAdmin)

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=86266049
DB_NAME=AMBIENTE DE HOMOLOGAÇÃO SF

# Porta em que o site vai rodar
PORT=3000

# Chave usada para assinar o token de login (pode ser qualquer texto grande e aleatório)
JWT_SECRET=nexus_homologacao_secret_key_86266049
